// eslint-disable-next-line react/prop-types
const Btn = ({ text }) => {
  return (
    <div>
      <button className="px-8 py-3 text-info bg-secondary">{text}</button>
    </div>
  );
};

export default Btn;
